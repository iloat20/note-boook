# 2026-05-02 日志

## Android Studio 国内加速配置

### 已完成
- **全局 Gradle 配置** (`~/.gradle/gradle.properties`):
  - JVM 堆从 2G 提升到 4G
  - 新增 `configureondemand=true`（按需配置）
  - 新增 `workers.max=4`（并行编译线程数）
  - 新增 `kotlin.daemon.jvmargs=-Xmx2048m`（Kotlin 守护进程独立内存）
  - 保留原有 daemon/parallel/caching 优化

### 已提供的工具
- **桌面脚本**: `android_studio_accelerate.bat` — 右键管理员运行即可
  - 添加 hosts 映射（dl.google.com → 203.208.40.125）
  - 刷新 DNS 缓存
  - 清除 Gradle 失败的下载缓存
  - 检查 Java 环境

### `accounting-app` 项目的现有配置（已验证无需改动）
- `settings.gradle.kts` → 阿里云镜像 ✅
- `gradle-wrapper.properties` → 腾讯云镜像 ✅
