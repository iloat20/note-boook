<view wx:if="{{tags.length > 0}}" class="strategy-tags-wrap" style="display: flex; flex-wrap: wrap; gap: 4px; margin-top: 6px;">
  <view wx:for="{{tags}}" wx:key="*this" class="{{size === 'mini' ? 'strategy-tag-mini' : 'strategy-tag'}}">{{item}}</view>
</view>
