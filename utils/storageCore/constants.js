/**
 * 常量定义
 * 统一从 utils/constants/index.js 导出，避免定义不一致
 */

const { MARKETS, TRANSACTION_TYPE, DEFAULT_STRATEGIES } = require('../constants/index')

module.exports = {
  MARKETS,
  TRANSACTION_TYPE,
  DEFAULT_STRATEGIES
}
